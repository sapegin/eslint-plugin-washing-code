import { rule as noUnnecessaryTemplateLiteralRule } from './no-unnecessary-template-literal.js';
import { rule as preferExplicitBooleanCheckRule } from './prefer-explicit-boolean-check.js';

export const rules = {
  'no-unnecessary-template-literal': noUnnecessaryTemplateLiteralRule,
  'prefer-explicit-boolean-check': preferExplicitBooleanCheckRule,
};
